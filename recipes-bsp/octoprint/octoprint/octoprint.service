[Unit]
Description=The responsive web interface for your 3D printer

[Service]
Type=simple
ExecStart=/usr/bin/octoprint --iknowwhatimdoing --config /etc/octoprint/config.yaml

[Install]
WantedBy=network-online.target
